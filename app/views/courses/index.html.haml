%h4 Courses

- @courses.each do |course|
  =course.name
  |
  =link_to "Show Students", course_path(course)
  -if current_user.admin?
    = link_to "Edit", edit_course_path(course)
    |
    = link_to "Delete", course_path(course), method: :delete
  %hr