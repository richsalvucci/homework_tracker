%h2 Sign in
= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
  %div
    = f.label :email 
    %br/
    = f.email_field :email, autofocus: true, input_html: {value:'user@test.com'}
  %div
    = f.label :password, input_html: {value: '12345678'} 
    %br/
    = f.password_field :password, autocomplete: "off"
  - if devise_mapping.rememberable?
    %div
      = f.check_box :remember_me
      = f.label :remember_me
  %div= f.submit "Sign in"
%br
%h2 Sign in
= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
  %div
    = f.label :email
    %br/
    = f.email_field :email, autofocus: true, input_html: {value: 'admin@test.com'}
  %div
    = f.label :password, input_html: {value: '12345678'}
    %br/
    = f.password_field :password, autocomplete: "off"
  - if devise_mapping.rememberable?
    %div
      = f.check_box :remember_me
      = f.label :remember_me
  %div= f.submit "Sign in"
= render "devise/shared/links"
